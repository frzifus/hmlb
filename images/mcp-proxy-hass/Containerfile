FROM ghcr.io/sparfenyuk/mcp-proxy:v0.10.0

USER root

# Install uv and hass-mcp
RUN apk add --no-cache uv && \
    uv tool install hass-mcp

ENV PATH="/root/.local/bin:${PATH}"

# Run as root since hass-mcp is installed in root's home directory
# USER nobody

EXPOSE 8080
