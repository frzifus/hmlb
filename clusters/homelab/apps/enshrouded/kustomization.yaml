---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: enshrouded

resources:
  - namespace.yaml
  - secret.yaml
  - pvc.yaml
  - server.yaml
  # Optional: playit.gg tunnel proxy (uncomment to enable)
  # - playit.yaml
